const Role = {
    admin: 'admin',
    student: 'student',
    faculty: 'faculty',
};

const UserStatus = {
    'in-progress': 'in-progress',
    blocked: 'blocked',
};

const UserConstants = {
    Role,
    UserStatus,
};

export default UserConstants;
